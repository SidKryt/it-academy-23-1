let str = `Hello`;
let bool = true;
let num = 13;

let strAndBoolSum = str + bool;
let strAndNumSum = str + num;
let numAndBoolSum = num + bool;

let strAndBoolMult = str * bool;
let strAndNumMult = str * num;
let numAndBoolMult = num * bool;

let strAndBoolDiv = str / bool;
let strAndNumDiv = str / num;
let numAndBoolDiv = num / bool;

let numFromStr = Number(str);
let numFromBool = Number(bool);

let strFromNum = String(num);
let strFromBool = String(bool);

let boolFromNum = Boolean(num);
let boolFromStr = Boolean(str);




